# -*- coding: utf-8 -*-
"""Per-certification configuration.

Each module here describes one exam: its display name (used in the prompts),
where its generated files live, its section headings, which slide formats to
download, and which environment variable holds its NotebookLM notebook ID.

Add a new exam by copying ccna.py and editing the values -- no changes to the
generation scripts are needed.
"""
